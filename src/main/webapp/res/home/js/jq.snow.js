/** * js网页雪花效果jquery插件 */(function ($) {    var snowInterval, id, className;    $.fn.snow = function (options) {        var $flake = $('<i>').css({'position': 'absolute', 'top': '-50px'}).addClass(options.className),            documentHeight = getWinSize().height,            documentWidth = getWinSize().width,            defaults = {                id: 'body',	//雪花显示区域                minSize: 10,		//雪花的最大尺寸                maxSize: 20,		//雪花的最大尺寸                newOn: 1000,		//雪花出现的频率                flakeColor: "#FFFFFF"            };        options = $.extend({}, defaults, options);        id = options.id;        className = options.className;        snowInterval = setInterval(function () {            var startPositionLeft = Math.random() * documentWidth - 100,                startOpacity = 0.5 + Math.random(),                size = random(options.minSize, options.maxSize),                endPositionTop = documentHeight - 40,                endPositionLeft = startPositionLeft - 100 + Math.random() * 500,                durationFall = documentHeight * 9 + Math.random() * 5000;            $flake.clone().appendTo('#' + options.id).css({                left: startPositionLeft,                opacity: startOpacity,                width: size,                height: size,                color: options.flakeColor,                'background-size': '100%'            }).animate({                top: endPositionTop,                left: endPositionLeft,                opacity: 0.2            }, durationFall, 'linear', function () {                $(this).remove();            });        }, options.newOn);    };    $.fn.snowStop = function () {        window.clearInterval(snowInterval);        $('#' + id).find('.' + className).each(function () {            $(this).remove();        })    };    //get window size    function getWinSize() {        if (window.innerWidth != 'undefined') {            return {width: window.innerWidth, height: window.innerHeight};        } else {            var win = document.documentElement;            return {width: win.clientWidth, height: win.clientHeight};        }    }})(jQuery);