/** * Created by Administrator on 2016/3/3. */var picIndex = 0;$(function () {    //设置轮播图区域的宽高    //var placeholder = $('.picScrollList a').eq(1).find('img').clone(true).css({    //	'position': 'relative',    //	'visibility': 'hidden'    //});    //$('.picScrollList').append(placeholder);    //banner轮播    var x = 750 / 260;    $('.picScrollBox').height($('#picScrollList a').eq(0).width() / x);//设置轮播图区域的宽高    $('#picScrollList a').eq(0).addClass('active');//默认显示第一张图    $('#picScrollList').everyTime('3s', 'picScrollList', function () {        $('.picScrollBox').height($(this).find('a').width() / x);//设置轮播图区域的宽高        var height = $(this).find('a').height();        var picTotal = $(this).find('img').length - 1;        if (picIndex < picTotal) {            picIndex++;        } else {            picIndex = 0;        }        $(this).find('a').eq(picIndex).addClass('active').siblings().removeClass('active');        $('.picScrollBtn span').eq(picIndex).addClass('active').siblings().removeClass('active');        $(this).css('top', height * -picIndex);    }, 0, true);    //设置页面的最小高度    $('.generalPlatArea').css('min-height', $(window).height());    //主页公告跑马灯-timer-1.2    var noticeIndex = 1;//网站公告滚动位置计数器    var height = $('.platNoticeList').find("li:first").height();    $('.platNoticeList').each(function () {        $(this).append($(this).find('li:first').clone(true));    });    //网站公告滚动    $('.platNoticeList').everyTime('3s', 'noticeScrollBox', function () {        $(this).everyTime('3cs', 'noticeScroll', function () {            if (noticeIndex == $(this).find('li').length || noticeIndex == 0) {                noticeIndex = 1;                $(this).css("top", '0px');            }            var h = parseInt($(this).css('top'));            h = h ? h : 0;            if (h == (-height * noticeIndex) && noticeIndex >= 0) {                $(this).stopTime('noticeScroll');                noticeIndex++;                return;            }            $(this).css("top", h - 1 + 'px');        }, 0, true);    }, 0, true);    //点击页面导航显示平台主导航    $('.mainMenuBtn').click(function () {        $('.generalPlatArea').css({            'left': '80%',            'height': $(window).height() + 'px'        });        $('#platMainNavBox').show().find('.platMainNavContent').height($(window).height() - 240);        $('body').css('overflow', 'hidden');        $('.platMainNavMask').attr('ontouchmove', 'stopMove(event)');        //window.ontouchmove = stopMove(event);        $('.fixBox').css('position', 'absolute');        $('.platMainNavMask').show();    });    //点击平台主导航右侧蒙版返回当前页面    $('.platMainNavMask').click(function () {        $('.generalPlatArea').css({            'left': '0',            'height': 'auto'        });        $('body').css('overflow', 'auto');        $('.platMainNavMask').removeAttr('ontouchmove');        //window.ontouchmove = '';        $('.fixBox').css('position', 'fixed');        $(this).hide();        $('#platMainNavBox').find('.platMainNavContent').height('auto');        $('#platMainNavBox').oneTime('6ds', function () {            $(this).hide();        });    });    //平台主导航页签切换    $('.platMainNav').click(function () {        $(this).next('ul').toggle();        $(this).parent().siblings().find('a').removeClass('active').next('ul').hide();        if ($(this).is('.active')) {            $(this).removeClass('active');        } else {            $(this).addClass('active');        }    });});//平台主导航显示时禁止拖动function stopMove(e) {    e.preventDefault && e.preventDefault();    e.returnValue = false;    e.stopPropagation && e.stopPropagation();    return false;}