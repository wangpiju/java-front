$(function () {    $('.nav a').on('click', function () {        $('.nav a').removeClass('act');        $(this).addClass('act');    });    var $pageHeader = $('.page-header');    $(window).on('scroll', function () {        var wTop = $(document).scrollTop();        $pageHeader.each(function () {            var offsetTop = $(this).offset().top;            var id = $(this).attr('id');            if ((wTop + 50) > offsetTop) {                //右侧内容标题浮动                var $clone = $(this).clone();                $('.head-fixed').remove();                $clone.addClass('head-fixed').removeClass('page-header').removeAttr('id');                $('#con').prepend($clone);                //左侧导航变色                $('.nav a').removeClass('act');                $('a[href="#' + id + '"]').addClass('act');            }        })    });})